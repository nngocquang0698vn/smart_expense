abstract final class VoiceNoteConfig {
  static const Duration recorderSampleInterval = Duration(milliseconds: 250);
  static const Duration clockUiTick = Duration(milliseconds: 200);
  static const int recorderSampleRate = 44100;
}
